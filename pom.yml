modelVersion: 4.0.0

groupId: org.maxur
artifactId: justj-core
version: 0.0.1-SNAPSHOT
name: 'Just(J)ava Core Library'

parent: {
        groupId: org.maxur,
        artifactId: justj-base,
        version: 0.1-SNAPSHOT
}

dependencies:
# Utils
  - { groupId: org.reflections, artifactId: reflections, version: 0.9.10 }

